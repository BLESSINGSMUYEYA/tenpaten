export default function Loading() {
    // This empty loading state allows the dashboard shell (sidebar, search, greeting) 
    // to render instantly, while specific content sections stream in via Suspense.
    return null;
}
